{
  "name": "geofence-monorepo",
  "version": "1.0.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=admin",
    "dev:test": "npm run dev --workspace=@geofence/test-app",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
